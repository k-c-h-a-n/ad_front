# ad_front

First, go to the frontend directory.
    ```
    cd front
    ```
        
Next, proceed with npm install.
    ```
    npm install
    ```
    
Next, run the frontend.
    ```
    npm start
    ```
